# 光标卫士 设计文档

## 1. 问题定义

### 要解决什么

AI 助手操作 macOS 桌面时，有两种投递方式：

| 投递 | 代码里的值 | 会不会动用户的鼠标 | 用户能不能察觉 |
|---|---|---|---|
| 语义投递 | `delivery: ax` | 不会 | 完全无感 |
| 真实投递 | `delivery: hid` | **会真的跳过去点** | 突然、吓人 |

真实投递发生在这些情况：点击落在坐标上（比如点文本框、点侧边栏这种没有 `AXPress` 能力的组）、以及后台投递失败后升级到前台重试。

用户的原话：「不要突然动我的光标，必须得让我知道。」

### 验收标准（什么叫完成）

1. 任何 `hid` 投递**之前**，刘海下方必须出现胶囊 toast
2. toast 文案是「N 秒之后光标将会移动」，N 从 3 倒数到 1
3. 倒数结束 toast 滑出，**之后**动作才发生
4. 虚拟光标（`delivery: pid`）的动作**不加任何延迟**，一次都不许拦
5. 预告失败（守护不在、超时）时，**拒绝执行**真实输入——宁可什么都不做

### 最容易翻车的地方

- **静默放行**：守护没起来就当作「没事」继续动光标，保护形同虚设 → 所以设计成 fail-safe，拿不到确认就拒绝
- **拦错对象**：把语义投递也拦了，等于把无障碍自动化整体拖慢 3 秒 → 只在 hid 路径上挂
- **倒计时与动作不同步**：调用方自己 sleep 3 秒而不是等守护回执，一旦守护卡住就会在没预告的情况下动手 → 由守护在倒数结束时回复放行
- **集成补丁被 `pi update` 覆盖**：补丁必须可重放，且要记录在记忆里

## 2. 方案

### 架构

```
pi-computer-use (Pi 扩展, TypeScript)
        │  发现本次 act 会走 hid 投递
        ▼
CursorGuard announce（CLI，同一二进制）
        │  Unix socket 一行 JSON
        ▼
光标卫士守护进程（LSUIElement .app）
        │  弹刘海胶囊，倒数 3→2→1
        ▼
倒数结束 → 回复放行 → 调用方执行真实输入
```

### 为什么是独立 app，而不是改 pi-computer-use 的 native helper

pi-computer-use 的 helper 是 Swift 编译的二进制，改它要重新编译 → cdhash 变化 → **TCC 的辅助功能与录屏授权会被 macOS 作废**（作者自己的文档里写了这个坑）。而且 npm 更新会覆盖。

独立 app 的代价是多一层 IPC，收益是：

- 不碰已经拿到授权的 helper，权限不用重授
- 任何自动化（不限于 pi-computer-use）都能调用它
- 升级 pi-computer-use 不影响守护进程

### 为什么复用褪黑素的技术而不是系统通知

系统通知在 ad-hoc 签名下不可用（`UNUserNotificationCenter` 直接拒绝），`osascript` 那条路在 macOS 26+ 只进通知中心不弹横幅、来源还是「脚本编辑器」图标。褪黑素已经验证过：自绘 NSPanel + NSGlassEffectView 是零授权、完全可控的唯一出路。

### 关键设计决定

| 决定 | 理由 |
|---|---|
| 守护在**倒数结束**时才回复放行 | 时序由守护单方控制，调用方不需要自己计时，不会和动画脱节 |
| `graceMs` 内连续请求直接放行 | 「后台失败→前台重试」属于同一个动作会话，重复倒数只是白等 3 秒 |
| 倒计时进行中到达的新请求 **等当前倒数结束** | 用户必须被真正告知过，不能因为并发就跳过预告 |
| 预告失败返回退出码 3 而非 0 | fail-safe：调用方据此必须放弃物理动作 |
| 只预告 hid，不预告 ax | 语义动作用户无感，拦了就是纯拖慢 |
| 键盘也算 | `hid` 的键盘输入会打进用户当前聚焦的应用，破坏力比动鼠标更大，同样要预告 |

## 2.5 胶囊的交互设计（一次返工记录）

第一版做成了「点整个胶囊取消」，并且在胶囊尾部加了「点按可取消」文字提示。用户指出两个问题：

1. **提示文字是多余的**，正确形态是 hover 时把文本区换成 × 图标
2. 更要紧的是：**我擅自把「通知」改成了「用户一动鼠标就取消」**——用户明确说这不对。用户的原话是「你别替我去做决定，你只用跟我澄清事实就行了。正确的做法，你得先问我」。他的语义是：**鼠标键盘在动，不构成取消理由**；只有当鼠标移到胶囊上（hover，表示「我在犹豫」）才暂停，移开就继续倒数。

定稿的交互（见 AGENTS.md §12b）：hover 暂停 + 移开继续 + 整个胶囊可点 + 按下有视觉反馈。这条是用户的设计，任何后续改动必须先问。

## 2.6 常驻进程瘦身：一进程改两进程（2026-09-19 用户要求）

用户要求「这个东西一定不能太重太大，必须得足够轻量」。实测发现重量全在 AppKit 上：

| 进程形态 | 私有物理内存（vmmap） |
|---|---|
| AppKit 进程 + 一个窗口 | 8.2–10.0MB |
| 原单进程守护（含 AppKit） | **15.6MB** |
| 不带 AppKit 的纯 socket 进程 | **1.8MB** |

`ps` 的 RSS（44–57MB）是骗人的：里面大部分是全系统共享的框架页。

方案：同一份源码用 `-D DAEMON` 条件编译出两个二进制。`Preannounce`（守护+CLI）不链接 AppKit，实测 **3.2MB**；`PreannounceToast`（GUI）由守护按需拉起，只在预告的数秒内存在。

**200ms 冷启动怎么消化（用户定的语义）**：toast 进程先把窗口滑入，倒数在滑入动画结束后才开始，所以用户看到的仍是完整的 N 秒，多出的冷启动时间只体现在调用方的等待里（实测 3 秒倒数 → 客户端 3.5 秒）。

## 3. 踩坑记录

（开发过程中追加）

- **NSWindow.animator() 在 LSUIElement 非激活窗口上不执行动画**——位置/透明度必须手动 Timer 逐帧驱动（继承自褪黑素）
- **高斯模糊要加在 label.layer 上**，加在容器层不生效（玻璃渲染管线不走容器层合成）
- **NSGlassEffectView 必须包裹全尺寸透明容器作 contentView**，只包 label 会变成细条
